import Root from './button.svelte'
import {
  buttonVariants,
  type ButtonProps,
  type ButtonSize,
  type ButtonVariant,
} from './button.svelte'

export {
  Root,
  buttonVariants,
  type ButtonProps,
  type ButtonSize,
  type ButtonVariant,
  //
  Root as Button,
}
